<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LearnEase</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="./css/styles.css" />
    <meta name="csrf-token" content="<%= csrfToken %>">
</head>
<body>
    <%- include('header.ejs') %>
   <div class=" d-flex justify-content-center container ">

   
        <form method="post" action="/login" class="row g-3 mt-2 w-50  p-5 rounded-3 border">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <h2><b>Login</b></h2>
        <div class="col-12">
          <label for="email" class="form-label"><strong>Email</strong></label>
          <input type="email"  required class="form-control" id="email" name="email" placeholder="123@org.com">
        </div>
        <div class="col-md-12">
          <label for="password" class="form-label"><b>Password</b></label>
          <input type="password" required class="form-control" id="password" name="password" placeholder="**********">
        </div>
        
        
        <div class="col-12 mt-5 d-flex justify-content-center">
          <button type="submit"  class="btn w-50 bold btn-primary">Take me In !</button>
        </div>
        <span >Don't have an account? <a  href="/signup">Register now</a></span> 
            <% if(messages && messages.error && messages.error.length > 0)  { %>
                <% for(var i=0; i<messages.error.length; i++) { %>
                  <span class="alert alert-danger"><%= messages.error[i] %></span>
                <% } %>
              <% } %>
        
     
       
      </form>
   

   </div>
</body>
</html>